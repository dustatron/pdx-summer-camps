import React from "react";

import { signIn, signOut, useSession } from "next-auth/react";

import { trpc } from "../utils/trpc";

const YourCamps = () => {
  return <div> YourCamps</div>;
};

export default YourCamps;
